#!/usr/bin/env python3
from urllib.parse import unquote_plus
import sys
for s in sys.argv[1:]:
    print(unquote_plus(s), end=" ")
print()
if len(sys.argv) > 1:
    exit(0)
for s in sys.stdin:
    print(unquote_plus(s), " ")
